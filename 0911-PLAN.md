# `logos` 견고화 계획 — 0911

> 목표: **테스트는 초록인데 렌더가 깨지는** 상태를 끝내고, 출판 교과서 수준(`DSL.md §13.8 "품질은 기본값"`)에 도달한다.
> 원칙: ⑴ 눈으로 보는 검증 루프를 먼저 세운다. ⑵ 좌표/뷰 코어를 단일 진실원으로 만든다. ⑶ IR은 순수 world 기하, 화면 의존 레이아웃은 Projector 레이어로 분리한다.

---

## ✅ 실행 결과 (2026-09-11 완료)

빠른 것 → 큰 것 순서로 **전 항목 처리**. `node --test test/*.test.js` → **137 pass / 0 fail**.

| 산출물 | 내용 |
|---|---|
| `backend/svg.js` | 그라디언트 오염 제거, 결정적 id, `dxPx/dyPx` 적용, `scaleX/scaleY` 원·타원, `math` 모드 |
| `core/scene.js` | `axesIR`/`polarGridIR` world·px 혼용 제거, `bounds()` 기반 `collect()`, polar view 정사각 정규화 |
| `shapes/curve.js` | adaptive 샘플링, 화면공간 불연속, 마칭스퀘어 스티칭(폐합), 라벨 배치 |
| `shapes/region.js` | `below`가 곡선 domain 상속, `bounds()` |
| `shapes/threeD*.js` | depth 기반 painter's algorithm |
| `backend/scene-ir.js` | `scaleX/scaleY`, `toPNG()`(resvg), `toHTML` 좌표 매핑 |
| `backend/katex.js` | `latexToText` (foreignObject 없는 폴백) |
| `test/invariants.test.js` | 무결성 린터 + 불변식 (신규) |
| `test/snapshots.test.js` + `test/fixtures/` | 골든 스냅샷 22개 (신규, fixtures는 gitignore·로컬 재생성) |
| `test/backend.test.js` | TikZ/Asymptote/JSXGraph/KaTeX/PNG (신규) |
| `test/scenes.js`, `test/COVERAGE.md` | 공용 씬 + 커버리지 매트릭스 (신규) |
| `examples/visual.js` | SVG→PNG 22개 (`output/png/`) (신규) → **5차에서 삭제**(`kit.saveFigures`) |
| `backend/fonts.js` + `assets/STIXTwoMath-Regular.ttf` | STIX Two Math 전역 폰트 (SVG @import / PNG 로컬) |
| `backend/hidden.js` | 3D hidden-line (깊이 버퍼) |
| `backend/layout.js` | 라벨 자동 배치 (충돌 회피) |
| `package.json` | `snap:update`, `parity9b`, `parity9c`, `examples`, `serve` (5차에서 `gallery`/`test:visual` 대체), optionalDependencies(resvg) |

**린터가 추가로 잡아낸 실제 버그**: `polarGridIR`의 `+4`(world+px 혼용) → 극좌표 각도 라벨이 `x=-265.9` 로 화면 밖.

---

## 0. 현재 상태 요약

- `node --test test/*.test.js` → **119 pass / 0 fail** (2026-09-11 확인).
- 그럼에도 `output/g_*.svg` 실제 렌더에 좌표 폭주·색 오염·미폐합 곡선·수식 소실이 존재.
- 원인: 테스트가 **문자열/구조 검사**(`svg.includes('#e11')`, 태그 개수, `!/NaN/`)뿐이고, **픽셀 좌표가 화면 밖으로 나가도, 도형이 안 보여도 통과**한다.

---

## 1. 진단 (파일:라인 근거)

### 증거 A — world 좌표와 pixel 오프셋 혼용 → 라벨이 화면 밖으로
`output/g_a1_point.svg` 실제 출력:
```xml
<text x="-21.6" y="-11459.99999999999" font-size="12" font-style="italic" ...>1.92</text>
```
원인 `core/scene.js`:
```js
// :332  y축 눈금 라벨
out.push(node('text', { x: zeroX - pad * 1.8, y: y + 4, ... }));   // '+4' 를 world 단위에 더함
// :339  원점 '0' 라벨
out.push(node('text', { x: zeroX - pad * 2.0, y: zeroY + 12, ... })); // '+12' 도 world 단위
```
world 범위가 `[1.9, 2.1]`(폭 0.2)일 때 픽셀 의도 오프셋 4가 world 단위로 더해져 y≈5.9 → 행렬 매핑 시 `-11460px`. 같은 파일에서 x축 눈금 라벨은 `y=621.6px`(600px 캔버스 밖, 클립).

**분류: 최상위 구조 결함 — world/pixel 표현 혼재.**

### 증거 B — 모든 채워진 원이 "씬 첫 도형 색" 그라디언트로 오염
`backend/svg.js`:
```js
// :58  씬 전체에서 첫 번째 fill 색 추출
const circColor = firstFill(nodes) || t.pointColor;
// :59  단일 radial 그라디언트 정의
... <radialGradient id="logoCircleGrad" ...>
// :157 원 채움에 무조건 적용
else if (d.fill && d.fill !== 'none') { fill = `url(#logoCircleGrad)`; }
```
- `g_a3_circle.svg`: 사용자가 지정한 `#dfe7fb` 평면 채움이 입체 음영 radial로 대체됨(교과서 원 아님).
- `g_d3_riemann.svg`: 원이 없는데도 gradient 색이 `#4dabf7`(리만 사각형) → 전역 오염.
- 채움색이 다른 도형이 여럿이면 **전부 첫 도형 색**으로 칠해짐.

### 증거 C — auto-world 프레이밍이 도형 종류를 모름
`core/scene.js:198-211` `collect()`는 `coords / vertices / sample / pointDir / center()+radius()`만 처리.
- `Ellipse`: `get center()`(함수 아님) + `semi` → 미수집.
- `Rectangle`·`Region`·`VectorField`·`Surface` → 미수집.
- 결과 `g_c1_ellipse.svg`: `semi(3,2)`인데 눈금이 `-6..6` → 기본값 `[-5,5]`로 잡혀 **도형이 절반 크기, 큰 여백에 방치**.

### 증거 D — non-equal에서 원/타원 반지름 오류
`backend/scene-ir.js:18-31`이 non-equal일 때도 `scale = Math.min(sx, sy)` 반환 → `backend/svg.js:158/173`이 `r*scale`, `rx*scale`에 사용. x/y 스케일이 다른 씬에서 원이 잘못된 반지름(타원이어야 할 것이 원).

### 증거 E — 곡선 엔진
- `shapes/curve.js:85-94`: 균일 샘플 `n=220` → `SENARIOS B1`("샘플 간격이 곡률에 반비례") 미충족.
- `shapes/curve.js:101`: `jumpThreshold = w`(정의역 폭). `x<0?-1:1`(점프 2, w=4) → **안 끊김** → 수직 연결선 아티팩트.
- `shapes/region.js:111-119`: `region.below(curve)`가 곡선 domain 무시, `[w.xmin, w.xmax]` 전체 음영 → #10/#44 음영이 곡선보다 넓게 삐져나옴.
- `shapes/curve.js:15-55` `implicitIR`(마칭스퀘어): 셀마다 2점 path 난발, 병합·폐합 없음 → `g_b5_implicit.svg` 꼬리가 `M..L..` 2점 path 수백 개(닫힌 곡선 아님).

### 증거 F — 비결정적 출력(골든 스냅샷 불가)
`backend/svg.js:34` `let CLIPN = 0, GRADN = 0;`(모듈 전역), `core/node.js:4` `NODE_ID`. 렌더마다 id가 달라져 `SENARIOS §L` 골든 비교가 원천 불가.

### 증거 G — 수식이 `<foreignObject><math>`에만 존재
`backend/svg.js:191,198`. resvg/Inkscape/Illustrator/LaTeX 파이프라인에서 **수식 소실** → `toPNG()/toPDF()` 실질 불가(현재 throw, `backend/scene-ir.js:92-100`).

### 증거 H — 그 밖(품질/정확)
- `backend/svg.js:183-184`: `point.dot().stroke(3)` → 흰 원 테두리(교과서 점 아님).
- 눈금 숫자 전부 `font-style="italic"` → 수치는 정립이 정석.
- `backend/scene-ir.js:73,75`: `toHTML()`이 world 좌표를 `px`로 사용 → 위치 어긋남.
- 3D: depth 정렬/hidden-line/silhouette 없음 → `SENARIOS K3`("곡선이 곡면 뒤로 가려짐") 미충족.
- `shapes/line.js` 등 `pickStyle`이 truthy 검사 → `opacity: 0`, `stroke: 0`이 탈락.

---

## 2. 왜 테스트가 못 잡는가

| 검사 유형 | 현재 | 결과 |
|---|---|---|
| 문자열 포함 | `svg.includes('#e11')` | 좌표가 폭주해도 통과 |
| 태그 개수 | `count(svg,'circle')` | 안 보여도 통과 |
| NaN 여부 | `!/NaN/` | `-11460`은 NaN이 아님 → 통과 |
| 골든/시각 회귀 | 없음(SENARIOS §L 미구현) | 회귀 감지 불가 |
| 기하 불변식 | 일부만(내심/접선 좌표) | 도형 자체는 검증 |

→ **관측 수단 부재가 근본 원인.** Phase 0가 먼저다.

---

## 3. 로드맵 (Phase 0 → 5)

핵심 설계 결정: **IR(`toIR`)은 순수 world 기하만 담고, 화면 의존 레이아웃(라벨 위치·눈금·오프셋)은 Projector를 가진 레이어에서 확정한다.**
현재는 text 노드가 world 좌표를 담고 emitter가 매핑하는데, **픽셀 오프셋을 표현할 방법이 없어** 증거 A가 발생했다.

### Phase 0 — 관측 가능한 피드백 루프 (최우선·전제)
- [x] **P0-1 결정성**: `CLIPN/GRADN`을 렌더 단위 리셋(`svg.js emitSVG`) → 같은 씬 = 같은 SVG.
- [x] **P0-2 SVG 무결성 린터**: `test/invariants.test.js` — 라벨/중심이 viewBox±margin 밖이거나 NaN/Inf 면 실패. **실제로 `polarGridIR`의 world/px 혼용 버그를 추가 검출.**
- [x] **P0-3 골든 스냅샷**: `test/fixtures/*.svg`(22개, **로컬 전용·gitignore**) + `npm run snap:update`. fixtures 부재 시 테스트는 skip(가짜 초록 방지).
- [x] **P0-4 시각 회귀 + toPNG**: `@resvg/resvg-js`(optionalDependencies), `SceneIR.toPNG()` 실제 구현(동적 import). **5차에서** `examples/visual.js` 는 삭제되고 `kit.saveFigures({ png:true, index:true })` 가 SVG+PNG+`index.html` 갤러리를 담당한다.
- [x] **P0-5 기하 불변식 테스트**: `test/invariants.test.js` (원/타원/implicit/영역/샘플링/결정성).

### Phase 1 — 좌표/뷰 코어 재설계 (구조적 토대)
- [x] **P1-1 Projector(px 오프셋 분리)**: `emitSVG`가 `dxPx/dyPx`를 map 이후 적용. `axesIR`/`polarGridIR`의 world+px 혼용 제거.
- [x] **P1-2 `bounds()` 프로토콜**: `Ellipse`/`Rectangle`/`Region`/`Segment`/`Curve(implicit)` 에 `bounds()`, `collect()`가 우선 사용 + `readCenter/readRadius` 견고화.
- [x] **P1-3 스케일 정합**: `_map()`이 `scaleX/scaleY` 반환, `<circle>`은 equal 일 때만, 아니면 `<ellipse>` 방출.
- [x] **P1-4 축 재작업**: 가장자리 inset, 경계 눈금 라벨 제외, 눈금 정립(`italic:false`), 라벨 화면 오프셋 px.

### Phase 2 — 곡선 엔진
- [x] **P2-1 adaptive sampling**: `segments(ctx)` 곡률 재귀 세분화(허용오차 ≈0.4px).
- [x] **P2-2 불연속 검출**: 뷰 높이의 절반 기준 점프 검출(step/asymptote 공통).
- [x] **P2-3 region domain**: `region.below`가 곡선 domain 상속.
- [x] **P2-4 implicit 정식화**: 표준 마칭스퀘어 + 세그먼트 스티칭 → 폴리라인/폐합(Z).
- [x] **P2-5 라벨 배치**: 접선 방향 오프셋 + px 오프셋.

### Phase 3 — SVG emitter 품질
- [x] **P3-1 그라디언트 정책**: 무조건 `logoCircleGrad`/`firstFill` 제거, `.gradient()` 명시 시에만.
- [x] **P3-2 수식 전략**: `toSVG({math:'text'|'foreignObject'|'path'})` + `latexToText` 폴백, `toPNG` 동작.
- [~] **P3-3 기타 품질**: `text` px 오프셋·클립 방지 완료. **점 마커/dash px 공간화는 기존 테스트 계약(A4/I1)과 충돌 → 보존(부분).**

### Phase 4 — 3D 레이어
- [x] **P4-1 depth 정렬**: `makeProjection`이 depth 반환, `polyline`이 평균 depth → z 정렬(painter's algorithm).
- [~] **P4-2 hidden-line/silhouette**: painter 정렬까지. 2D 곡선의 곡면 가림은 미구현(부분).
- [x] **P4-3 `dim(3)` 뷰/카메라 일관성**: 3D 정사각 equal 강제, depth 기반 z.

### Phase 5 — 백엔드 정합 & CI
- [x] **P5-1 `toTikZ()/toAsymptote()` 검증**: `test/backend.test.js` (TikZ/Asymptote/JSXGraph/KaTeX/PNG + adapt 네임스페이스).
- [x] **P5-2 커버리지 매트릭스**: `test/COVERAGE.md`.
- [x] **P5-3 CI 게이트**: 골든(`snapshots`) + 린터/불변식(`invariants`) + 백엔드(`backend`)가 `npm test`에 포함.

---

## 4. Quick Wins (즉시 눈에 보이는 개선, 반나절) — ✅ 전부 적용

| # | 위치 | 수정 | 효과 |
|---|---|---|---|
| QW1 | `core/scene.js:332,339` | `y+4`, `zeroY+12` → px 오프셋(`dxPx/dyPx`)으로 | y축/원점 라벨 복구 |
| QW2 | `backend/svg.js:58-59,157` | 무조건 gradient 제거 | 원이 교과서처럼 평면 채움 |
| QW3 | `shapes/curve.js:101` | 불연속 임계값 px 기반 | step 함수 수직선 아티팩트 제거 |
| QW4 | `shapes/region.js:111-119` | `below`가 곡선 domain 상속 | 음영 삐져나옴 제거 |
| QW5 | `backend/svg.js:34`, `core/node.js:4` | 전역 카운터 리셋/hash | 결정적 출력, 골든 가능 |

> Quick Wins는 증상을 없애지만, **Phase 0-2(린터)가 먼저 있어야 재발을 막는다.**

---

## 5. 검증 전략

1. **단위**: 기하 불변식(내심/외심/접선/폐곡선) — 기존 유지·확장.
2. **불변식 린터**: emit 좌표 경계/NaN 검사(신규).
3. **골든 스냅샷**: 결정적 SVG 문자열 비교 + `snap:update`.
4. **시각 회귀**: PNG 래스터 저장, 사람이 검토 + (후속) 픽셀 diff.
5. **E2E**: 갤러리 생성 — **5차에서** `examples/gallery.js`(삭제) → `kit.saveFigures({ index: true })` 가 `output/*/index.html` 생성(`SENARIOS §O`).

---

## 6. 우선순위/결정 포인트

Phase 0는 공통 전제. 이후 1차 목표에 따라 순서가 갈린다.

- **A) 브라우저 SVG 충실도**: Phase 0 + Phase 1 + Quick Wins — 화면이 먼저 예쁘게.
- **B) 출력물 신뢰성**: Phase 0 + Phase 3(수식/emitter) + `toPNG` — 인쇄·삽입 가능하게.
- **C) 수학적 정확도**: Phase 0 + Phase 2 — 곡선/영역부터.
- **D) 전체 순서대로**: Phase 0→5 전부.
- **E) Quick Wins만**: 눈에 보이는 개선 먼저 확인 후 재논의.

> 권장: **Phase 0(관측) → Quick Wins → Phase 1(코어)**. 관측 없이 손대면 재발하고, 코어 없이 곡선/3D를 손대면 같은 버그가 재생산된다.

---

## 7. matplotlib(`example.py`) 품질 재현

`example.py`(14D1A/16C1A, matplotlib)의 그림을 **logos 만으로** 재현 가능한지 검증.

- 재현 스크립트: `examples/mpl_parity.js`(→ 5차에서 정리, 이후 `mpl_parity_9b.js`/`_9c.js` 로 계승) → `output/parity/*.svg` + `*.png`.
  - `14d1a-1-circle-trade`(a1), `16c1a-1-circle-area`(b1) 재현 완료.
- 추가한 figure 레벨 API:
  - `scene.title(t)` / `.xlabel(t)` / `.ylabel(t)` / `.legend(loc)`
  - `annotate.text(point).label(...).anchor().offset().font().bold()`
  - `Drawable.font()/bold()`, emitter: `bold`, `rotate`, 멀티라인(`\n`→`tspan`), 수식 중앙정렬
  - `latexToText`에 `\tfrac/\dfrac`, `\mathrm/\text` 추가
- **결과**: 2D 미적분/기하 그림은 matplotlib와 구조적으로 동등(원·접선·점·영역채움·슬라이스·화살표주석·범례·제목·축라벨·KaTeX 수식). SVG(브라우저)는 KaTeX, PNG(resvg)는 유니코드 폴백.
- **1차 남은 갭 → 추가 구현 완료 (2026-09-11)**:
  - (1) **자동 라벨 배치** — `scene.layout()` + `backend/layout.js` (텍스트 충돌 회피).
  - (2) **subplots/패널** — `panels([figs], { cols, cell, title })` (중첩 svg + suptitle + PNG).
  - (3) **`fill_betweenx`·`barh`·링/부채꼴** — `region.betweenX`/`barH`/`annulus`/`wedge` (+ emitter `rect`).
  - (4) **3D** — `surface.z().mesh()/faces()`(painter 정렬 + 음영), `vectorField3()`(quiver).
- 재현 추가: `output/parity/` 에 two-slicings(패널), bars-rings(barh/링/부채꼴), saddle-field(3D 곡면+quiver), layout-auto(자동배치).
- **2차 요청 반영 (2026-09-11)**:
  - **폰트**: STIX Two Math 를 **전역**(텍스트·수식·축) 적용 — `backend/fonts.js`. SVG 는 Google Fonts `@import`(XML 이스케이프 처리), PNG(resvg)는 `assets/STIXTwoMath-Regular.ttf` 로컬 폰트, HTML 은 `<link>`.
  - **spine/grid 세밀 제어**: `spines({top,right,bottom,left,width,color})`, `grid({ alpha, width, minor, minorWidth })`.
  - **3D hidden-line/실루엣**: `backend/hidden.js` — faces 로 깊이 버퍼를 만들고, `hiddenTest` 선(mesh/quiver)의 가려진 구간을 제거. `surface.z().mesh()` 는 전환점만 방출해 op 폭증 방지. 정의역 경계는 실루엣으로 강조.
  - **panels tight**: `panels(..., { tight:true })`.
  - **버그 수정**: `stroke(0)`·`opacity(0)` 이 기본값으로 덮이던 문제(truthy 검사 → `undefined`/`??`), `line.pickStyle`, emitter 반올림.
- **3차 요청 반영 (2026-09-11) — `example1.py`(9B 2D 기하) 재현**:
  - 재현 스크립트: `examples/mpl_parity_9b.js` → `output/parity9b/*.svg` + `*.png` (**25개 figure 전부 생성**).
  - `9b-line-forms`(2×3 패널), `step-*`(단계별 1×3 / 2×3), `parallel-perpendicular`, `angle-between-lines`, `midpoint-division`, `point-line-distance`, `two-lines-distance`, `point-circle-distance`, `tangent-lines-circle`, `*-details`(circle/ellipse/parabola/hyperbola), `conic-identification`(순서도), `conic-comparison`(2×2), `parametric-motion`(2×2), `step-parametric`(2×3), `triangle-area`, `area-polygon`, `point-reflection`.
  - **추가 API**: `point.marker('circle'|'square'|'triangle'|'diamond'|'star'|'point'|'plus'|'cross', {size,open})` (+`.size()`), `annotate.text(...).box({facecolor,alpha})`(bbox 배경), `.rotate(deg)`. emitter: `markerSvg()`, `textBoxSvg()`.
  - **주의**: `panels()` 는 **컴파일된 SceneIR** 만 받는다(원시 `Scene` 아님). 셀 크기와 서브 씬 `.size()` 를 일치시켜야 겹치지 않는다(기본 600×600). → **5차에서 자동화**: `cell` 미지정 시 figure `.size()` 최댓값 사용, `kit.subplots()` 가 원시 Scene 을 자동 컴파일.
- **4차 요청 반영 (2026-09-11) — `example2.py`(9C 3D 기하) 재현**:
  - 재현 스크립트: `examples/mpl_parity_9c.js` → `output/parity9c/*.svg` + `*.png` (**35개 figure 전부 생성**).
  - 3D 프리미티브 신규 (`shapes/threeD3.js`): `curve3.parametric(f).on([t0,t1])` / `curve3.through([P,…])`, `arrow3(from, to)`(quiver; 머리 길이 = `ratio`·화살표길이, 기본 0.12), `surfaceParam((u,v)=>[x,y,z]).on(ur,vr).wire(nu,nv)` / `.solid(nu,nv)`(법선 램버트 명암 + painter 정렬) / `.cmap(name)`.
  - 간이 컬러맵 `cmapColor(name,t)`: `viridis`·`plasma`·`coolwarm`·`jet`·`summer` (5–6 stop 선형 보간). `surface.z(...).cmap(name)` 로 ZSurface 에도 적용.
  - 카메라: `scene.camera({ elev, azim, distance })` (mplot3d `view_init` 대응), `scene.camera({ aspect:[ax,ay,az] })` (mplot3d `box_aspect` [4,4,3] ≈ [1,1,0.75] 대응, 정사영 전 축 스케일).
  - `scene.axes(false)` 로 3D 자동축(axes3IR) 생략 가능 — 예제는 직접 `arrow3` 축을 그린다.
  - `annotate.text(point(x,y,z))` 가 3D 점을 카메라 투영(2D 는 기존과 동일).
  - `Sphere`/`Plane`/`Cylinder`/`Cone`/`Surface`·`curve3`·`arrow3`·`surfaceParam` 에 `vertices` getter 추가(자동 프레이밍).
  - `world3From()` 을 원점-중심 반경 방식 → **투영 bbox + 8% 여백** 방식으로 변경(mplot3d 박스 프레이밍에 근접). 스냅샷 `E1_sphere` 갱신 필요.
- **여전히 부분**: hidden-line 은 quad 단위 painter + 깊이버퍼(per-pixel 완전 정합 아님), 라벨 배치는 그리디, 점 마커/dash 픽셀화는 테스트 계약상 보존.
- **9C 부분 항목**: 원근(perspective) 투영 미지원(정사영만), 3D 축 눈금 라벨은 자동축 사용 시에만, `plot_surface` 의 per-face cmap 은 높이(z) 기준 근사, 3D 텍스트는 회전/bbox 없이 2D 평면 렌더.
- **5차 요청 반영 (2026-09-11) — 예제 정리 · 공용 헬퍼 승격 · 타이포그래피** (상세: `KIT.md`, `README.md`):
  - **예제 정리**: `mpl_parity_9b.js`(2D 25 figure) · `mpl_parity_9c.js`(3D 35 figure) **2개만 대표 예제로 유지**. 나머지 조잡한 예제 삭제 —
    `examples/adapters.js` · `book.js` · `gallery.js` · `interface.js` · `mpl_parity.js` · `v02.js` · `visual.js`.
    (`gallery.js`/`visual.js` 의 갤러리 기능은 `kit.saveFigures({ index: true })` 로 대체, `npm run examples` 가 `output/*/index.html` 생성)
  - **`kit.js` 신설** (`export * as kit from './kit.js'`): `palette` · `plot2d(xr,yr,opts)` · `plot3d(opts)` · `subplots(figs,opts)` ·
    `saveFigure/saveFigures/writeGallery` · `seg` · `poly3`. 예제 2개에서 복사돼 있던 색 팔레트/씬 프리셋/패널 합성/저장 루프를 전부 키트로 이동.
  - **3D 도우미 승격** (`shapes/threeD3.js`, `index.js` export): `axes3({length,color,width,labels,origin,ratio})`(mplot3d 축, 배열 반환 →
    `.add(...axes3())`), `quadrics.{plane,ellipsoid,ball,hyperboloid1,hyperboloid2,cone,cylinder}`(모두 `surfaceParam` 상속 →
    `wire/solid/cmap` 사용), `circle3(r,z,center)`, `frame3(xlim,ylim,zlim)`(그리지 않고 **프레이밍에만** 참여하는 상자 = `set_xlim/ylim/zlim`),
    `Arrow3.ratio(r)`.
  - **`panels()` 셀 크기 자동화**(`backend/scene-ir.js`): `cell` 미지정 시 figure 들의 `.size()` **최댓값**을 셀 크기로 사용 →
    “셀 크기와 서브씬 크기가 어긋나 패널이 겹치던” 3·4차의 함정 제거. 두 예제에서 `cell: PCELL`/`PCELL3` 를 삭제.
  - **타이포그래피(행간·자간)**: `backend/fonts.js` 의 `TYPE = { lineHeight: 1.32, letterSpacing: 0.01 }`.
    - 기본 행간 1.15 → **1.32**(멀티라인 `<tspan dy>` + bbox 높이 동시 반영), 자간 `0.01em` 을 `<style>` 로 전역 적용.
    - 개별 오버라이드: `Drawable.lineHeight(x)` · `Drawable.letterSpacing(px)` (`annotate.text(...).label('a\nb').lineHeight(1.5)`).
    - `index.js` 에 `typography`(=TYPE) export, 테스트 `features.test.js` 에 행간/자간 계약 추가.
  - **검증**: `npm test` **155 pass / 0 fail**(신규 5개 — 3D 도우미 · kit 프리셋/팔레트 · subplots 셀 추론 · saveFigures/갤러리 · 타이포그래피).
    리팩터링 전후 출력 비교(텍스트·박스 제외, 기하만)로 **기하 동일** 확인: 9B 24/25, 9C 25/35 —
    차이가 나는 figure 는 모두 의도된 것(`frame3` 8꼭짓점 프레이밍, 패널 셀 자동화로 600×600 서브씬이 칸 안에 들어감, 2D 패널 셀 통일).
  - **문서**: `README.md`(저장소 안내·문서 지도·실행법) · `KIT.md`(키트 API·3D 도우미·타이포그래피·FAQ·변경 이력) 신설,
    `INTERFACE.md §13` API 지도에 kit/3D 도우미 추가, `test/COVERAGE.md` 갱신.

